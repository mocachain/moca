# Legacy in-tree module — removed in PR-2 (cosmos/evm migration)

Replaced by `github.com/cosmos/evm` (x/vm / x/feemarket); precompile helpers moved to `x/evm/precompiles/types`, ante alias to `app/ante/evmiface`. Kept here so the additions PR stays additions-only; deleted in the follow-up deletions PR.
