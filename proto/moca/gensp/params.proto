syntax = "proto3";
package moca.gensp;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "github.com/mocachain/moca/v2/x/gensp/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "evmos/x/gensp/Params";
  option (gogoproto.goproto_stringer) = false;
}
